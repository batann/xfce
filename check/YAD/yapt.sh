--field="cmus":fbtn "sudo apt install cmus" ;;
--field="flatpak":fbtn "sudo apt install flatpak" ;;
--field="git":fbtn "sudo apt install git" ;;
--field="phython-pip":fbtn "sudo apt install phython-pip" ;;
--field="taskwarrior":fbtn "sudo apt install taskwarrior" ;;
--field="timewarrior":fbtn "sudo apt install timewarrior" ;;
--field="sweeper":fbtn "sudo apt install sweeper" ;;
--field="chatgpt":fbtn "sudo apt install chatgpt" ;;
--field="exhuberant-ctags":fbtn "sudo apt install exhuberant-ctags" ;;
--field="vit":fbtn "sudo apt install vit" ;;
--field="neovim":fbtn "sudo apt install neovim" ;;
--field="pandoc":fbtn "sudo apt install pandoc" ;;
--field="manhtml":fbtn "sudo apt install manhtml" ;;
--field="bleachbit":fbtn "sudo apt install bleachbit" ;;
--field="openssh-server":fbtn "sudo apt install openssh-server" ;;
--field="openssh-client":fbtn "sudo apt install openssh-client" ;;
--field="renameutils":fbtn "sudo apt install renameutils" ;;
--field="mat":fbtn "sudo apt install mat" ;;
--field="ffmpeg":fbtn "sudo apt install ffmpeg" ;;
--field="buku":fbtn "sudo apt install buku" ;;
--field="megatools":fbtn "sudo apt install megatools" ;;
--field="links":fbtn "sudo apt install links" ;;
--field="trash-cli":fbtn "sudo apt install trash-cli" ;;
--field="tmux":fbtn "sudo apt install tmux" ;;
--field="yad":fbtn "sudo apt install yad" ;;
--field="nodau":fbtn "sudo apt install nodau" ;;
--field="pwman":fbtn "sudo apt install pwman" ;;
--field="bwmw-ng":fbtn "sudo apt install bwmw-ng" ;;
--field="vnstat":fbtn "sudo apt install vnstat" ;;
--field="turtle note":fbtn "sudo apt install turtle note" ;;
--field="festival":fbtn "sudo apt install festival" ;;
--field="espeak":fbtn "sudo apt install espeak" ;;
--field="festvox-us-slt-hts":fbtn "sudo apt install festvox-us-slt-hts" ;;
--field="fzf":fbtn "sudo apt install fzf" ;;
--field="rofi":fbtn "sudo apt install rofi" ;;
--field="ddgr":fbtn "sudo apt install ddgr" ;;
--field="tldr":fbtn "sudo apt install tldr" ;;
--field="newsboat":fbtn "sudo apt install newsboat" ;;
--field="falkon browser":fbtn "sudo apt install falkon browser" ;;
--field="kodi":fbtn "sudo apt install kodi" ;;
--field="xterm":fbtn "sudo apt install xterm" ;;
